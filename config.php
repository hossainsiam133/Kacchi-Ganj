<?php
// Global configuration for Kacchi-Ganj app
// Change DEFAULT_ADMIN_ID here if your admin user has a different id in the `users` table.

if (!defined('DEFAULT_ADMIN_ID')) {
    define('DEFAULT_ADMIN_ID', 1);
}
